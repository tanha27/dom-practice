/*function handleAdd(){
    var element = document.getElementById('balance');
    var currentValue = parseInt(element.innerText);
    var sum = currentValue + 1;
    element.innerText = sum;
}


function handleDelete(){
    var element = document.getElementById('balance');
    var currentValue = parseInt(element.innerText);
    var sub = currentValue - 1;
    element.innerText = sub;
}*/




function handleAdd(){
var element = document.getElementById('balance');
var currentValue = parseInt(element.innerText);
 var sum = currentValue + 1;
 element.innerText = sum;
}




function handleDelete(){
    var element = document.getElementById('balance');
    var currentValue = parseInt(element.innerText);
    var sub = currentValue - 1;
    element.innerText = sub;
}


























